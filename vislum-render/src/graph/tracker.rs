// ResourceStateTracker is now simplified since AutoCommandBuffer handles transitions
#[derive(Default)]
pub struct ResourceStateTracker {
    // Empty for now - AutoCommandBuffer tracks image states internally
}
